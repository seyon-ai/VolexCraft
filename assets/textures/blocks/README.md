Drop block texture PNGs here. See the main README's "Image files" section
for the exact filenames the game looks for. Any file you don't add just
falls back to the built-in procedural texture — nothing breaks.
Recommended: square images, 128x128 (or 64x64), no transparency needed
except tall_grass.png which should have a transparent background.
